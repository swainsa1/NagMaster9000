[project]
name = "nagmaster9000"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.44",
    "requests>=2.31",
    "python-dotenv>=1.0",
]

[tool.uv]
dev-dependencies = []
